export enum MODE_LOGIN {
  LOGIN = "login",
  REGISTER = "register",
}

export enum SCREEN_LOGIN {
  LOGIN = "LOGIN",
  REGISTER = "REGISTER",
  FORGET_PASS = "FORGET_PASS",
}

export enum TYPE_INPUT {
  TEXT = "text",
  PASSWORD = "password",
}
